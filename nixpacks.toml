[phases.setup]
nixPkgs = ["php84", "php84Extensions.mysqli", "php84Extensions.pdo", "php84Extensions.pdo_mysql", "php84Extensions.mbstring", "php84Extensions.openssl"]

[phases.install]
cmds = ["echo 'PHP extensions installed'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
